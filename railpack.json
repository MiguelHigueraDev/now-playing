{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "install": {
    "cmds": [
      "cargo build --release -p api"
    ]
  },
  "deploy": {
    "startCommand": "./target/release/api"
  }
}
