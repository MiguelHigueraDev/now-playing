{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "install": {
    "cmds": [
      "cargo build --release"
    ]
  },
  "build": {
    "cmds": [
      "mkdir -p bin",
      "cp target/release/api bin/"
    ]
  },
  "deploy": {
    "startCommand": "./bin/api"
  }
}
